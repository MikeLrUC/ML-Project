function main()
    preprocess("hello", true);
end


function preprocess(root, save)
    % preprocess images and save them for the next time avoiding spending
    % time in preprocessing
    disp(save);
end
